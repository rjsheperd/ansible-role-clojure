---
dependency:
  name: galaxy
driver:
  name: docker
lint:
  name: yamllint
platforms:
  - name: ${platform_name:-"centos7"}
    image: ${platform_image:-"centos:7"}
provisioner:
  name: ansible
  lint:
    name: ansible-lint
